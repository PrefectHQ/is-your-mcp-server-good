[project]
name = "is-your-mcp-server-good"
version = "0.1.0"
description = "one prefect api served four ways: the demos behind the webinar of the same name"
requires-python = ">=3.12"
dependencies = [
    "fastmcp[code-mode]>=4.0.2",
    "httpx2",
]

[tool.uv]
package = false
